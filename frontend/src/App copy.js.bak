import React, { useState } from "react";
import RequestUserInfo from "./components/UserInfo.js";
import ShoppingList from "./components/test.js";
import MyButton from "./components/button.js";
import FruitTable from "./components/FruitTable.js";
import { BeginDate, EndDate } from "./components/datePicker.js";

function App() {
  const [count, setCount] = useState(0);

  function handleClick() {
    setCount(count + 1);
  }
  return (
    <div>
      {/* <MyButton count={count} clickHandler={handleClick} />
      <RequestUserInfo />
      <ShoppingList />
      <FruitTable /> */}

      <BeginDate />
      <EndDate />
    </div>
  );
}

export default App;

// https://velog.io/@jjunyjjuny/React-useState는-어떻게-동작할까
// https://react.dev/learn

